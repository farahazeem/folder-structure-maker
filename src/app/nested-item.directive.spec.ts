import { NestedItemDirective } from './nested-item.directive';

describe('NestedItemDirective', () => {
  it('should create an instance', () => {
    const directive = new NestedItemDirective();
    expect(directive).toBeTruthy();
  });
});
